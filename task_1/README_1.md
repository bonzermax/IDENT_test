**Вариант 1:**
- Удобство
использования
(простота запросов): Запросы без join
- Расширяемость
(добавление новых
ролей): Тяжело в масштабировании, т.к. для каждой нового типа персоны потребуется новая таблица
- Целостность (FK на
приёмы,
расписание):  При обновлении данных надо будет изменять данные в каждой из таблиц т.к. данные дублируются
- Производительность
(JOIN-ы, индексы):  Быстрые запросы
- Нормализация /
дублирование
данных:  ФИО и телефон хранятся дважды

**Вариант 2:**
- Удобство
использования
(простота запросов): Требуется join для получения полных данных
- Расширяемость
(добавление новых
ролей): При добавлении новой роли берётся внешний ключ на таблицу persons, поэтому отсутствует дублирование данных
- Целостность (FK на
приёмы,
расписание):  Данные изменяются в одном месте
- Производительность
(JOIN-ы, индексы):  
- Нормализация /
дублирование
данных:  Все данные хранятся по одному разу, нормализация полная

**Вариант 3:**
- Удобство
использования
(простота запросов): Легко для чтения
- Расширяемость
(добавление новых
ролей):  Решается путём добавления новых колонок, но и появляется больше пустых полей
- Целостность (FK на
приёмы,
расписание):  
- Производительность
(JOIN-ы, индексы):  Очень большая таблица, особенно если будут вводиться новые роли/поля
- Нормализация /
дублирование
данных:  Нарушается 1nf, т.к. есть null поля